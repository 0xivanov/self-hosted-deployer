name: tls-api
image: example/tls-api:3.1.0
service:
  port: 8443
  health:
    path: /healthz
routing:
  domain: tls.example.test
deploy:
  replicas: 2
placement:
  arch: linux/arm64
secrets:
  - API_KEY
  - DATABASE_URL
state:
  mode: stateless
