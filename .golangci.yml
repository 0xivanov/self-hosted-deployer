version: "2"

run:
  timeout: 5m

linters:
  enable:
    - bodyclose
    - errcheck
    - gosec
    - govet
    - ineffassign
    - misspell
    - nolintlint
    - revive
    - sqlclosecheck
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports
